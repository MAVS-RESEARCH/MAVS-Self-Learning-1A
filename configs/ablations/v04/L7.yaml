schema_version: "1.0.0"
id: L7
group: persistence_consolidation
reference_id: L0
isolated_factor: persistence_payload
toggle: {persistence_payload: query_policies_only}
unchanged_fields: [locked_banks, seeds, retained_byte_budget, ledgers, runtime, metrics]
causal_question: Is reusable evidence acquisition valuable independent of diagnostic growth?
expected_direction: query_yield_gain_without_full_diagnostic_gain
required_metrics: [query_yield, rounds, evidence_unavailable_escalation, uar, frr]
pass_fail_interpretation: Improvement supports query-policy memory; no gain limits that claim.
